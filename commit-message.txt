fix: Resolve Extension Marketplace issues

This commit fixes several issues with the Extension Marketplace integration:

1. JSON Parsing Error:
   - Updated marketplace configuration to use raw GitHub content URLs
   - Fixed content type handling for API requests

2. Extension Installation:
   - Added server-side implementation for extension installation API
   - Implemented proper error handling in extension installation
   - Added support for marketplace extension installation

3. UI Improvements:
   - Reset mock data (ratings, downloads) to realistic initial values
   - Removed duplicate extension entries in the marketplace
   - Added proper loading and error states
   - Improved empty state handling for new extensions

4. Security and Performance:
   - Added proper authentication checks for admin API endpoints
   - Implemented file download and validation for extensions
   - Added placeholder implementation for extension package handling

This ensures the marketplace can properly fetch extensions from the GitHub
repository and users can install extensions directly from the marketplace.
